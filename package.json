
{
  "name": "poc",
  "version": "1.0.0",
  "description": "PoC to get the API information using puppeteer",
  "type": "commonjs",
  "scripts": {
     "test": "jest" 
    },

  "devDependencies": {
    "jest": "^27.4.5",
    "jest-puppeteer": "^6.0.3",
    "puppeteer": "^13.0.1"
  }
}
